# Default smoke profile.
# Use checks-cloud.yaml or checks-local.yaml directly when needed.
apps_required:
  - app-of-apps
  - infra-app-of-apps
  - operators-app-of-apps
  - security-app-of-apps
  - security-namespaces
  - cert-manager
  - envoy-gateway
  - httpbin

apps_optional:
  - apps-app-of-apps
  - sops-age-seed
  - argocd-runtime
  - argocd-projects

apps_common_networking_required:
  - tailscale-operator
  - external-dns
  - cloudflared-apps
  - exposure-demo

apps_common_networking_optional:
  - openclaw

namespaces_required:
  - argocd
  - cert-manager
  - demo

namespaces_optional:
  - ai

namespaces_common_networking_required:
  - network
  - tailscale

pods_required_namespaces:
  - cert-manager
  - demo

pods_optional_namespaces:
  - ai

pods_common_networking_required_namespaces:
  - network
  - tailscale
