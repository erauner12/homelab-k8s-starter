apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-tailscale
  annotations:
    tailscale.com/proxy-class: "iptables-proxy"
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: app
      port:
        number: 80
  tls:
    - hosts:
        - app
