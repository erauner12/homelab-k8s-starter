apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Security-layer namespaces
# This kustomization manages namespaces for security infrastructure:
# - auth: Authentication services (Dex, OAuth2 Proxy)
# - security: Security tools (1Password Connect, Kyverno admission)
# - external-secrets: External Secrets Operator

resources:
  - _system/
  - _apps/
