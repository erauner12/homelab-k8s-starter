apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-apps-token
  namespace: network
type: Opaque
stringData:
  # Cloudflare connector token for the managed tunnel
  cf-tunnel-token: "replace-with-cloudflare-tunnel-token"
