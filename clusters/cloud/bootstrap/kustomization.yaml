apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cloud cluster bootstrap - root Applications for ArgoCD
# Apply this to bootstrap the cloud cluster:
#   kubectl apply -k clusters/cloud/bootstrap/

resources:
  - app-of-apps.yaml
  - operators-app-of-apps.yaml
  - security-app-of-apps.yaml
  - infra-app-of-apps.yaml
