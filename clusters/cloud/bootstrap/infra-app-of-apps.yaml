apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-app-of-apps
  namespace: argocd
  labels:
    homelab.dev/component: "argocd"
    homelab.dev/layer: "infra"
    homelab.dev/managed-by: "argocd"
    homelab.dev/cluster: "cloud"
  annotations:
    argocd.argoproj.io/sync-wave: "-200"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: infrastructure
  source:
    repoURL: git@github.com:erauner/homelab-k8s.git
    targetRevision: master
    path: clusters/cloud/argocd/infrastructure
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: false       # CRITICAL: Never auto-prune infrastructure app-of-apps
      selfHeal: true
    syncOptions:
    - CreateNamespace=false
    - ApplyOutOfSyncOnly=true
    - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
