apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: security-namespaces
  namespace: argocd
  labels:
    homelab.dev/layer: security
    homelab.dev/category: namespaces
  annotations:
    argocd.argoproj.io/sync-wave: "-89"
spec:
  project: infrastructure
  source:
    repoURL: git@github.com:erauner/homelab-k8s.git
    targetRevision: master
    path: ./security/namespaces/overlays/erauner-cloud
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=false
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
