apiVersion: v1
kind: Secret
metadata:
  name: longhorn-backup
  namespace: longhorn-system
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: longhorn-backup
  AWS_SECRET_ACCESS_KEY: <minio-secret-key>
  AWS_ENDPOINTS: https://minio.erauner.synology.me
  AWS_BUCKET_REGION: us-east-1        # dummy, satisfies Longhorn parser
  # Optional – only if you use a self-signed cert
  # AWS_CERT: |
  #   -----BEGIN CERTIFICATE-----
  #   <PEM of your MinIO TLS cert/CA>
  #   -----END CERTIFICATE-----
